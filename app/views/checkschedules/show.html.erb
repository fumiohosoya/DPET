<%= render 'admin/backtoadmin' %>

<% daystr = ["Mon", "Tue", "Wed", "Thr", "Fri", "Sat", "Sun"] %>



<div class="col-12 bg-white" style="line-height:0.8rem;">
    <h3 class="bg-white text-dark"> Check Schedule Maker</h3>
<% if @schedules.any? %>
<%# @schedules.each do |schedule| %>
  <%#= schedule.dayofweek %>
<%# end %>

<div class="col-12">
    <div class="col-6">
        <%= link_to "Reset_to_default", make_default_checkschedule_path(@company_id), class:'btn btn-warning btn-lg w-100' %>
    </div>
    <div class="col-6">
        <%= link_to "Edit", edit_checkschedule_path(@company_id), class:'btn btn-warning btn-lg w-100' %>
    </div>
</div>
<table class="table table-striped table-bordered">
    <tr>
       <td colspan="7" class="bg-grey text-center weekhead">
           1st week
       </td>
    </tr>
    <%= render 'weekcommon' %>
    <%= render 'week51p' %>

   <tr>
       <td colspan="7" class="bg-grey text-center weekhead">
           2nd week
       </td>
    </tr>
    <%= render 'weekcommon' %>
    <%= render 'week52' %>
    <tr>
       <td colspan="7" class="bg-grey text-center weekhead">
           3rd week
       </td>
    </tr>
    <%= render 'weekcommon' %>
    <%= render 'week53p' %>
    <tr>
       <td colspan="7" class="bg-grey text-center weekhead">
           4th week
       </td>
    </tr>
    <%= render 'weekcommon' %>
    <%= render 'week52' %>
</table>

</div>


<script>
    $("#day6select select").on("change", function(e) {
        
        var target = $(e.target).attr("id");
        var value = $(e.target).val() - 1;
        
        console.log(target);
        console.log(value);
        
        var text = $(e.target.options[value]).text();
        $("." + target + "m").text(text);
        debugger;
    })
    
</script>

<% end %>